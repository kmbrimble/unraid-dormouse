**Dormouse**

Hot-file tiering for Unraid — detects sustained reads on slow storage, promotes the surrounding working set to fast storage, and returns it when it goes cold. Phase 1 of this release installs the plugin, registers its settings page, and runs a no-op supervised daemon; it moves nothing yet.
